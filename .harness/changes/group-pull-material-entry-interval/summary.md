# 变更记录：拉群营销拉料间隔配置

- 日期 / 分支 / worktree: 2026-07-27 / `1.0.1-snapshot` / 当前 checkout
- 需求来源: Armada 拉群营销逐个拉料与随机间隔改造
- 状态: 已部署第一套测试环境

## 目标（一句话）

在拉群营销新建抽屉中提供可手动选择的拉料间隔，并在详情中明确展示实际上下 20% 的随机范围。

## 缺口拆解 / 任务清单

- [x] 创建表单新增 1～60 的整数分钟输入，默认 5 分钟。
- [x] 页面即时提示实际随机等待范围，例如 5 分钟对应 4～6 分钟。
- [x] 提交时将分钟转换为后端字段 `materialEntryIntervalSeconds`。
- [x] 任务详情展示已保存间隔和随机范围。
- [x] 补充默认值、边界校验、FormData 配置和页面结构测试。

## 关键设计决策

- 页面使用分钟便于人工配置，接口统一使用秒，避免后端调度精度受前端展示单位限制。
- 随机范围文案由共享函数生成，新建抽屉和任务详情复用同一规则。
- 沿用现有拉群营销新建抽屉，不新增路由或另一套重复创建流程。

## 验证（evidence-before-done）

- 相关 Node 测试 11 条通过。
- `pnpm typecheck`：通过。
- 本次相关文件 ESLint、Vue 文件 Stylelint：通过。
- `pnpm build`：通过，构建产物 3.88 MB。

## 部署

- 2026-07-27 通过隔离的临时发布目录部署到 `test1 / 第一套环境`，仅带入本功能前端文件，
  未带入本地既有的 `src/utils/http/index.ts` 修改。
- 部署脚本退出码为 0；nginx 已重建并运行，内置检查确认页面可访问且环境标识为“第一套环境”。
- 未 commit。

## 遗留 / 跟进

- 无。
