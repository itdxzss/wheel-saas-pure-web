# 通讯录营销回执统计与任务结果

2026-09-10，用户授权在主仓 `1.0.3-snapshot` 实施（起始 eaf2a50），最初不 commit/push；用户后续已追加发布授权，现已部署 test1。

列表执行进度改为已处理/计划，与累计发送确认、送达、已读分开展示。单个结果抽屉提供概览、账号数据和联系人明细，支持累计/互斥回执筛选、失败/未知/跳过原因筛选、账号排序与服务端分页。CSV 与列表复用指标；旧账号失败数不再称封号数。旧联系人抽屉和无调用的前端旧查询方法已删除。

自动刷新每 10 秒串行执行；抽屉关闭、页面后台或 KeepAlive 停用时停止。列表选中待删除记录、编辑或查看结果时暂停列表刷新。UNKNOWN 不显示单勾；准备中、零计划和统计缺失均有独立提示。无路由/权限/数据库结构变更，无生产假数据。

验证：130 项相关 Node 测试；完整 tsc/vue-tsc；定向 ESLint/Stylelint/Prettier；Vite 生产构建；2 项 Playwright（本地合成 API，含真实列表和结果抽屉、CSV、跨页筛选、排序、迟到回执、关闭/后台停刷新、全 UNKNOWN、390px 窄屏）。本地图片已检查，无页面错误或全页横向溢出。

跨仓接口、统计定义、复现命令、验证范围与剩余真机验收见 [后端变更记录](../../../../armada/.harness/changes/2026-09-10-contact-marketing-receipt-metrics.md)。本地 E2E 夹具位于 `e2e/fixtures/contact-receipts`，仅测试使用，不是生产入口。

## 追加发布授权

用户已追加授权提交、推送并部署第一套环境 test1。发布前已把列表有收件人账号数统一为 stats.accounts.readyAccountNum，并补充页面断言。实际发布成功：产品提交 `69c8560` 已推送，test1 前端部署 SUCCESS。实际 HTTP 首页、通讯录页面和统计 JS 的内容哈希均与本地构建一致；新 API 未登录返回 40104。后端产品提交 `92223696`。未进行登录态与真机回执验收，详细运行观察见后端变更记录。
