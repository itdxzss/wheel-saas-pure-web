# 剧本营销：分组选号与逐群检查

- 状态：已部署 test1；隔离候选 f9489933 本地提交，未推送。
- 范围：公共素材复用、角色与逐项间隔、推手分组下的群搜索、缺口报告和独立进群入口、提交进度与真实结果。
- API：新增 accountGroupId、roleKey 和逐项等待；检查与启动错误返回完整群报告。
- 约束：保存草稿不发送；检查不自动启动；切换筛选不删除已选群；任一已选群不达标则整个任务不启动。
- 暂停恢复：保留原绑定、命令与进度，不保留未提交下一项的剩余等待。
- 进群：保存草稿后导航到既有 /task/join，不自动创建或启动进群任务。
- 权限：沿用租户菜单与 script_marketing 权限，不添加生产 mock。
- 三菜单：公共消息素材（图片与超链共用）、可复用角色剧本、独立任务副本。素材提供新增/编辑/复制，剧本定义另有启停和软删除；没有添加素材删除入口。
- 管理员沿用手动选择，推手逐群随机分配；角色绑定与原提交/结果在任务详情展示。
- 验证：typecheck、生产 build、聚焦 ESLint / Prettier / Stylelint 通过；form.test.ts 5/5；playwright.script-marketing.config.ts 本地 API 夹具页面测试 5/5。页面截图已查看。
- 页面用例：素材到剧本再到任务、缺口报告与重新检查不自动启动、角色顺序保存、切换任务的迟到详情/记录隔离。真实环境另完成建群 147/148、独立进群 74、营销任务 2 双群八条协议发送成功。
- 页面按消息编辑、角色编排、定义选择、资格报告、创建和详情拆分；列表约 410 行，保留在同一文件以集中管理列表/抽屉请求与操作状态，未超 600 行限制。
- 实测修复四个页面/抽屉的 v-auth 为 v-perms；移除人工 meta.auths 后，5 条 E2E 及相关 lint/格式检查通过。真实素材按钮、任务终态、分组及群名筛选均验证。
- 不含引用回复、可配置跳过或每日重复。实际制品和业务证据见后端 .harness/changes/script-marketing-refinement/acceptance-20260910.md；协议发送成功不代表所有成员已接收或已读。
