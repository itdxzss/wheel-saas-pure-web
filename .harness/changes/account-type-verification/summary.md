# 账号类型上线校验前端变更

- 日期: 2026-08-30
- 状态: 代码完成，待随 Armada 部署

## 目标

导入时明确申报个人号/商业号，账号列表展示协议校验状态和自动纠正结果。

## 任务

- [x] 取消导入抽屉默认个人号并补必填校验
- [x] API 模型接收申报类型、校验状态和校验时间
- [x] 账号列表展示未校验、已确认、已纠正、无法确认状态
- [x] 更新单测并运行 typecheck/build

## 验证

- 聚焦测试：3 suites / 26 tests 通过。
- `pnpm typecheck` 通过。
- `pnpm build` 通过。

## 约束

- 列表业务筛选继续使用后端返回的当前有效账号类型。
- 前端不自行推断账号类型，只展示后端真值和校验状态。
