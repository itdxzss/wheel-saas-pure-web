# 变更记录：拉群营销一级任务列表 V16

- 日期 / 分支 / worktree: 2026-07-29 / `1.0.1-snapshot` / 当前 checkout
- 需求来源: 《拉群营销\_PRD需求文档\_V1.2》第 16 章
- 状态: 设计已确认，待实施

## 目标（一句话）

将现有拉群营销一级页面升级为五项查询条件和九个合并字段的任务列表，同时兼容后端暂未返回的新版统计字段。

## 范围

- [ ] 查询区展示任务 ID、任务名称、任务状态、阻塞原因、资源状态。
- [ ] 一级任务列表按 PRD 第 16 章展示九个合并字段。
- [ ] 新版统计字段缺失时显示 `--`，不添加静态演示数据。
- [ ] 保留现有分页、新增抽屉、详情跳转和任务操作。
- [ ] 补充查询、表格结构、缺失值及回归测试。

## 关键设计决策

- 直接升级现有 `group-pull-marketing` 页面，不新增重复菜单或路由。
- 新版统计字段先以可选类型接入，后端未返回时只降级对应单元格。
- 九个字段保持合并展示，复杂统计不拆成额外独立列。
- 生产页面不使用静态、随机或 mock 任务数据。

## 验证（evidence-before-done）

- 待实施完成后补充测试、类型检查、lint/stylelint 与构建结果。

## 遗留 / 跟进

- 后端聚合统计字段和最终响应命名不在本次前端页面范围内。
