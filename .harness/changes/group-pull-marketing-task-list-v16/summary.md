# 变更记录：拉群营销一级任务列表 V16

- 日期 / 分支 / worktree: 2026-07-29 / `1.0.2-snapshot` / 当前 checkout
- 需求来源: 《拉群营销\_PRD需求文档\_V1.2》第 16 章
- 状态: 前端已完成，待后端统计字段契约与目标环境联调

## 目标（一句话）

将现有拉群营销一级页面升级为五项查询条件和九个合并字段的任务列表，同时兼容后端暂未返回的新版统计字段。

## 范围

- [x] 查询区展示任务 ID、任务名称、任务状态、阻塞原因、资源状态。
- [x] 一级任务列表按 PRD 第 16 章展示九个合并字段。
- [x] 新版统计字段缺失时显示 `--`，不添加静态演示数据。
- [x] 保留现有分页、详情跳转和任务生命周期操作。
- [x] 新增入口改为独立创建页，替换原创建抽屉。
- [x] 补充查询、表格结构、缺失值、跨页选择及回归测试。

## 关键设计决策

- 直接升级现有 `group-pull-marketing` 页面，不新增重复菜单或路由。
- 新版统计字段先以可选类型接入，后端未返回时只降级对应单元格。
- 九个字段保持合并展示，复杂统计不拆成额外独立列。
- 生产页面不使用静态、随机或 mock 任务数据。
- 执行阶段和最近执行时间缺失时统一显示 `--`，不使用旧资源状态推断。
- 创建页使用隐藏静态 `/create` 路由，并排在 `/:id` 之前。

## 验证（evidence-before-done）

- 拉群营销相关 Node 测试通过。
- `pnpm typecheck` 通过。
- 定向 ESLint 与拉群营销 Vue 文件 Stylelint 通过。
- `pnpm build` 生产构建通过。
- `git diff --check` 通过。

## 遗留 / 跟进

- 后端聚合统计字段和最终响应命名不在本次前端页面范围内。
- 创建页的目标数据解析、候选群组、资源账号、营销模板和提交接口仍待契约确认。
- 自动化会话无可用浏览器实例，桌面与窄屏视觉验收需在目标环境补做。
