# 拉群任务 GROUP_MARKETING 原型纠正

## 目标

把 `拉群列表原型.jpg` 与 `拉群详情.jpg` 对应的字段和布局实现到“拉群任务”菜单，并恢复此前被误改的独立“拉群营销”菜单。

## 设计

- 设计文档：`docs/superpowers/specs/2026-07-30-pull-task-group-marketing-prototype-correction-design.md`
- 列表路由名称：`TaskPull`（当前菜单路径为 `/task/pull`）
- 正确新建路由：`/task/pull-task/create`
- 固定任务类型：`GROUP_MARKETING`

## 边界

- 前端实现，不修改后端和协议层。
- 新表单不猜测映射旧创建接口。
- 恢复 `group-pull-marketing` 误改时不改写历史。
- 不触碰用户现有的群列表相关未提交改动。

## 当前进度

- [x] 确认根因与两个菜单边界
- [x] 用户确认纠正方案
- [x] 形成纠正设计
- [x] 用户审阅书面设计
- [x] 编写实施计划
- [x] 恢复拉群营销误改
- [x] 实现拉群任务列表与新建页
- [x] 补齐条件字段、本地账号筛选、配置校验和任务预览
- [x] 完成验证
- [ ] 重新部署第一套环境（按用户要求，本轮不部署）

## 验证结果

- 拉群任务及相邻“拉群营销”回归测试：51/51 通过。
- TypeScript：`tsc --noEmit` 通过。
- Vue TypeScript：`vue-tsc --noEmit --skipLibCheck` 通过。
- 生产构建：`vite build` 通过。
- 未调用后端创建接口，未执行部署或推送。

## 实现提交

- `339ff31f`：恢复此前误改的独立“拉群营销”菜单。
- `5fb97c40`：添加拉群任务新建页隐藏路由。
- `b6313cb8`：补齐拉群任务原型统计字段。
- `e402b177`：按原型调整拉群任务列表布局。
- `13f466ff`：添加拉群任务营销草稿模型。
- `9207a586`：实现拉群任务营销新建页面。
- `c2d4cc4d`：补齐创建页交互并修正列表返回路由。
