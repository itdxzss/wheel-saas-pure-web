# 速拉群模式/建群营销页面

## 范围
- 新增建群营销任务页面与 API 封装。
- 支持账号分组、料子文件、营销模板、发送间隔配置。
- 支持任务列表查看、停止任务、详情查看。

## 约束
- 页面只调用 `src/api/group-creation-marketing.ts`。
- UI 组件沿用 Element Plus / pure-admin-thin。
- 多余料子文件只展示未匹配，不阻止创建；没有任何账号文件匹配时阻止创建。
