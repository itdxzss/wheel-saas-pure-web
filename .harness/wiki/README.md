# Wiki

这里放 wheel SaaS 前端迁移后的事实摘要，避免 Agent 每次都从旧项目和后端文档里重新猜。

建议文件：
- `菜单与路由.md`：tenant 菜单 key、path、component、perm_key 映射。
- `接口协议.md`：前端使用到的 SaaS API 摘要。
- `页面迁移清单.md`：旧 `wheel-saas-web` 到新项目的页面迁移状态。

本目录只放已确认事实，不放临时猜测。

